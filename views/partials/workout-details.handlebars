<div class="workoutcard">
  <a href="/workout/{{id}}">
    <header> {{name}}</header>

    {{! <div class="card-body"> }}

    <img src="/images/{{filename}}" width="500px" />

  </a>
  <p>Benefits of Workout: {{description}}</p>
  <p>Reps/Sets: {{reps}}</p>
  <p>Difficulty Level: {{difficulty_level}}</p>
  <p>Equipment Needed: {{equipment}}</p>
  <p>Posted By: {{username}}</p>
</div>